# Normalize line endings so the repo is consistent across machines/OSes.
* text=auto

# Treat binary assets as binary (never line-ending normalized).
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
